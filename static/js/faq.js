let infos = {
    "item1": ":0"
};

function toggle(id) {
    let item = document.getElementById(id);
    if (item == undefined) {
        return;
    } 

    let state = item.getAttribute("state");
    
}